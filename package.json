{
  "name": "research-designer-tokens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DTCG design tokens + Style Dictionary build for research-designer. Additive token layer — demos remain single self-contained HTML and inline a copy of build/variables.css.",
  "scripts": {
    "build:tokens": "node build-tokens.mjs"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0"
  }
}
