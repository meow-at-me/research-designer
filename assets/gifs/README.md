# assets/gifs

Looping captures of the motion/3D demos for the README **GIF row** (see Improvement
Brief Part 2.2). Motion and 3D don't survive a static screenshot — these are the
selling point, so record short loops and wire them into the README `<!-- GIF ROW -->` slot.

Target files (drop them here):

- `liquid-chrome.gif` — the real chrome torus-knot (three.js), 3–5s loop
- `prism-glass.gif` — frosted glass over the living aurora shader, 3–5s loop
- `experiment-dashboard.gif` — charts drawing in + the `file:line` source-trace hover

Also worth making: one **before/after still** (default-agent output vs. spec-applied
output) — the format that travels best on Reddit / HN.

Keep each loop a few seconds and reasonably small (≤ a few MB).
